import { style } from '@vanilla-extract/css';

export const layer = style({
  padding: '16px 24px',
});

export const imageLayer = style({
  width: 81,
  height: 75,
  overflow: 'hidden',
  borderRadius: 12,
});
